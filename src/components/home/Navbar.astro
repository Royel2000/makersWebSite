---
import { Image } from "astro:assets";
import logo from "../../assets/MK.jpg";
import ThemeToggle from "./ThemeToggle.astro";
---

<header
  id="navbar"
  class="fixed top-0 left-0 right-0 z-50 flex justify-center pointer-events-none overflow-hidden pb-2 transition-all duration-500"
>
  <div
    id="notch-container"
    class="relative pointer-events-auto flex items-start transition-all duration-500 ease-[cubic-bezier(0.16,1,0.3,1)]"
  >
    <svg
      class="w-5 h-5 -mr-px shrink-0 self-start"
      viewBox="0 0 20 20"
    >
      <path d="M20 0H0C11.0457 0 20 8.95431 20 20V0Z"></path>
    </svg>

    <nav
      id="dynamic-notch"
      aria-label="Global"
      class="flex items-center gap-4 p-1.5 rounded-b-[20px]"
    >
      <a href="#aboutUs" class="flex items-center gap-2.5 group">
        <div
          class="relative flex items-center justify-center overflow-hidden rounded-full h-8 w-8 shrink-0 border border-[var(--border-strong)] group-hover:border-[var(--accent)] transition-colors duration-300"
        >
          <Image
            src={logo}
            alt="Makers Logo"
            loading="eager"
            class="h-full w-full object-cover transition-transform duration-300 group-hover:scale-110"
            width={60}
            height={60}
          />
        </div>

        <div class="flex items-center gap-1.5">
          <span class="text-sm font-bold tracking-wider text-[var(--text-primary)]"> MAKERS </span>
          <span class="hidden sm:inline text-sm font-medium text-[var(--text-muted)]">
            • FOTO & VIDEO
          </span>
        </div>
      </a>

      <div class="h-4 w-px bg-[var(--border-strong)]"></div>

      <ThemeToggle />

      <a
        href="#footer"
        class="inline-flex items-center gap-1.5 text-xs font-semibold px-3.5 py-1 text-[var(--accent)] hover:text-[var(--text-primary)] bg-[rgba(var(--accent-rgb),0.1)] hover:bg-[var(--accent)] rounded-full border border-[rgba(var(--accent-rgb),0.4)] hover:border-[var(--accent)] transition-all duration-300 active:scale-95"
      >
        <span>Contacto</span>
        <span
          aria-hidden="true"
          class="text-[11px] transition-transform duration-300 group-hover:translate-x-0.5"
          >&rarr;</span
        >
      </a>
    </nav>

    <svg
      class="w-5 h-5 -ml-px shrink-0 self-start"
      viewBox="0 0 20 20"
    >
      <path d="M0 0H20C8.95431 0 0 8.95431 0 20V0Z"></path>
    </svg>
  </div>
</header>

<style is:global>
  #notch-container {
    --nav-ease: cubic-bezier(0.16, 1, 0.3, 1);
    transform-origin: top center;
  }

  html.dark #notch-container {
    --nav-color: #000000;
  }
  html.light #notch-container {
    --nav-color: #ffffff;
  }

  #notch-container > svg {
    fill: var(--nav-color);
  }

  #dynamic-notch {
    background: var(--nav-color);
    transition: background 0.4s var(--nav-ease);
  }

  html.dark #navbar.scrolled #dynamic-notch {
    background: #000000;
  }
  html.light #navbar.scrolled #dynamic-notch {
    background: #ffffff;
  }

  #navbar.scrolled #notch-container {
    transform: scale(0.97);
  }

  #notch-container:hover {
    transform: scale(1.02);
  }

  @media (prefers-reduced-motion: reduce) {
    #notch-container,
    #dynamic-notch {
      transition: none !important;
    }
  }
</style>
