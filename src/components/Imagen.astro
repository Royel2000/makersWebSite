---
import { Image } from "astro:assets";
const { img, alt, width, height, class: className } = Astro.props;
---

<Image
  src={img}
  alt={alt}
  class={className ?? "w-full rounded-xl object-cover"}
  loading="lazy"
  width={width ?? 800}
  height={height ?? 600}
/>